# Local dev database (SQLite) and misc scratch
*.sqlite
*.sqlite-journal

# Python bytecode (never commit)
__pycache__/
*.pyc

# Editor toolchain (build with editor/README instructions)
node_modules/
